# Domain invariants

Rules the product may never violate. **An invariant with no enforcing test is a wish.**

Each row names the test or check that fails when the rule is broken. Add a citation check (every
`Enforced by` path resolves) together with the first row.

| ID | Invariant | Enforced by | Since |
|---|---|---|---|
| INV-1 | | `path/to/test` | |
